#include<stdio.h>
int power(int,int);
void main()
{
int i,n,x;
printf("Enter x and n:");
scanf("%d%d",&x,&n);
printf("%d",power(x,n));
}
int power(int x,int n)
{
    if(n==0)
    return 1;
    else
    return (x*power(x,n-1));
}
